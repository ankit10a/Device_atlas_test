{
	"name": "device_atlas-test",
	"version": "1.0.0",
	"description": "For handle the incoming inbound call",
	"main": "server.ts",
	"scripts": {
		"build": "npx tsc",
		"serve": "node dist/server.js",
		"start": "npm run build && npm run serve",
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "nodemon src/server.ts"
	},
	"author": "Ankitverma",
	"license": "ISC",
	"dependencies": {
		"callassistance-aiagent": "file:",
		"dotenv": "^16.4.7",
		"express": "^4.21.2"
	},
	"devDependencies": {
		"@types/express": "^4.17.21",
		"@types/node": "^20.12.7",
		"nodemon": "^3.1.9",
		"ts-node": "^10.9.2",
		"typescript": "^5.4.5"
	}
}